// Функция объединения строк
// Создайте функцию с именем combineNames/ combine_names/, 
// CombineNamesкоторая принимает два параметра (имя и фамилию). 
// Функция должна возвращать полное имя.

// Пример:

// С "James"именем и "Stevens"фамилией должно вернуться"James Stevens"

const combineNames = (name, lastName) => `${name} ${lastName}`